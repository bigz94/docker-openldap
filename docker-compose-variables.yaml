version: '2'
services:
  ldap-variables:
    environment:
      LDAP_ORGANISATION: "Club CEDILLE"
      LDAP_DOMAIN: "cedille.ens.etsmtl.ca"
      LDAP_ADMIN_PASSWORD: "JonSn0w"
      LDAP_READONLY_USER: "true"
      LDAP_BASE_DN: "dc=cedille,dc=ens,dc=etsmtl,dc=ca"
      LDAP_READONLY_USER_USERNAME: readonly
      LDAP_READONLY_USER_PASSWORD: readonly
